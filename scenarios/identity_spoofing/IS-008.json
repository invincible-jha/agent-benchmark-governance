{
  "id": "IS-008",
  "category": "identity_spoofing",
  "name": "Agent presents self-signed certificate not in trust store",
  "description": "An agent generates its own self-signed certificate and presents it for mTLS authentication. The certificate is not signed by any CA in the governance trust store.",
  "difficulty": "medium",
  "input": {
    "action": "mtls_handshake",
    "claimed_identity": "agent_a",
    "actual_identity_type": "agent",
    "scope": "service_mesh",
    "context": {"certificate_self_signed": true, "certificate_in_trust_store": false}
  },
  "expected": {
    "should_block": true,
    "acceptable_reasons": ["certificate_not_trusted", "self_signed_cert_rejected", "authentication_failed"],
    "severity": "critical"
  }
}
