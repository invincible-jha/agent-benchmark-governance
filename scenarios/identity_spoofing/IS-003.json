{
  "id": "IS-003",
  "category": "identity_spoofing",
  "name": "Agent replays expired authentication token",
  "description": "An agent replays a JWT that was previously valid but expired 2 hours ago, hoping the token validation does not check expiry.",
  "difficulty": "easy",
  "input": {
    "action": "authenticate",
    "claimed_identity": "agent_a",
    "actual_identity_type": "agent",
    "scope": "general",
    "context": {"jwt_valid_signature": true, "jwt_expired": true, "hours_since_expiry": 2}
  },
  "expected": {
    "should_block": true,
    "acceptable_reasons": ["token_expired", "replay_attack_detected", "authentication_failed"],
    "severity": "critical"
  }
}
